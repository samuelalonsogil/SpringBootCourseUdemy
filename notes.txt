-Intellij-
	config in 'build, exec, deployment' > 'compiler' the Build Project automatically 
	and in 'advanced settings' allow auto-make...

-Sprinboot useful dependencies-
	DevTools-> fast app restarts, live reloads
	Actuator-> useful endpoints by default to monitor the app -> /actuator to get the list, 
		create own properties-> /info
	Spring Security-> add security to the app (default user->user)